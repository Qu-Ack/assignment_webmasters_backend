import { Request, Response } from "express";
import { User } from "../schemas/users";





export async function adminHandler(req: Request, res:Response) {
    console.log("from rolehandler to admin handler")

}